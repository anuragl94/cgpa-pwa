{
  "name": "cgpa-pwa",
  "version": "1.0.0",
  "description": "The PWA for CGPA website",
  "main": "/index.html",
  "homepage": "https://anuragl94.github.io/cgpa-pwa/",
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "https-localhost": "^4.6.4",
    "parcel-bundler": "^1.12.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf .cache && rm -rf dist",
    "start": "npm run clean && parcel index.html",
    "build": "npm run clean && parcel build index.html",
    "deploy": "npm run build && gh-pages -d dist -t true"
  },
  "author": "Anurag Lingam",
  "license": "ISC"
}
